export const setPosts = posts => {
    return {
        type: "SET_POST_ACCEPTED",
        payload: posts
    }
}

export const setPosts = posts => {
    return {
        type: "SET_POST_REJECTED",
        payload: posts
    }
}